---
/*
  Landing page campagne Google Ads : location de vehicule avec chauffeur.

  Intention de recherche differente de l'accueil. Ici le visiteur cherche a
  LOUER UN VEHICULE AVEC CHAUFFEUR (autocariste Nantes, location minibus
  Nantes, location autocar Nantes), pas un coordinateur d'evenement. Le
  vocabulaire de la page reste donc "location autocar / bus / minibus avec
  chauffeur" et jamais "transport evenementiel".

  Contraintes d'audience issues de la campagne :
    - 83 % du trafic est mobile : tout se concoit mobile d'abord
    - audience majoritairement 45-65 ans : lisibilite, contraste, cibles
      tactiles genereuses, pas d'interaction subtile

  Le hero est ecrit ici et non dans un composant partage : il ne ressemble
  pas au hero de l'accueil et n'a pas vocation a etre reutilise.
*/
import Base from '../layouts/Base.astro';
import Section from '../components/Section.astro';
import CallBar from '../components/CallBar.astro';
import ButtonArrow from '../components/ButtonArrow.astro';
// Telephone : une seule source, SITE_DATA, pour le lien d'appel comme pour
// l'affichage, ici comme dans la barre d'appel. Le NAP publie ne peut donc
// pas diverger de la fiche Google Business.
import { SITE_DATA, TEL_HREF, TEL_AFFICHE } from '../data/site.ts';
// Cle Web3Forms et page de confirmation : memes valeurs que le formulaire de
// l'accueil, importees et non recopiees. Voir le bloc FORMULAIRE plus bas.
import { WEB3FORMS_KEY, PAGE_ENVOI } from '../lib/devisEnvoi.ts';

const title = 'Location autocar, bus et minibus avec chauffeur à Nantes';
const description =
  'Location d\'autocar, bus, minibus et van avec chauffeur à Nantes et en Loire-Atlantique. De 3 à 120 places. Devis sous 24h.';

// Ligne de reassurance sous les CTA.
const REASSURANCE = [
  'Devis sous 24h',
  'Chauffeurs professionnels',
  'Nantes et Grand Ouest',
];

/*
  Vehicules mobilisables.

  ATTENTION, contrainte juridique et Google Ads, pas un choix de style :
  aucun vehicule n'est detenu en propre. Le modele est la coordination de
  transporteurs partenaires. Sont donc INTERDITS sur toute la page, y
  compris dans les meta et le JSON-LD : "notre flotte", "nos vehicules",
  "nos chauffeurs", "notre parc". Toute formulation possessive sur le
  materiel ou les conducteurs serait une allegation intenable.

  Formulations utilisables : "vehicules de nos transporteurs partenaires",
  "nous mobilisons", "nous selectionnons le vehicule adapte", "chauffeurs
  professionnels de nos partenaires".

  Aucune photo : montrer un autocar qui n'est pas celui qu'on fournit pose
  exactement le meme probleme que le vocabulaire possessif. La capacite
  porte donc seule la charge visuelle de chaque carte.

  La capacite est composee en trois morceaux pour que seul le nombre soit
  gros : "jusqu'a" + "3" + "passagers" se lit comme une phrase ordinaire,
  mais le 3 seul porte le poids typographique.

  `plage` distingue une fourchette (15 a 22) d'un plafond (3, 8, 120) : la
  premiere occupe six caracteres au lieu d'un a trois et passe donc un cran
  en dessous, sinon la ligne se casse dans la grille a deux colonnes.
*/
const VEHICULES = [
  {
    nom: 'Berline',
    prefixe: 'jusqu\'à',
    nombre: '3',
    plage: false,
    description:
      'Transferts VIP, dirigeants, intervenants. Gare, aéroport, hôtel.',
  },
  {
    nom: 'Van',
    prefixe: 'jusqu\'à',
    nombre: '8',
    plage: false,
    description:
      'Petits groupes, équipes, transferts aéroport Nantes Atlantique.',
  },
  {
    nom: 'Minibus',
    prefixe: 'de',
    nombre: '15 à 22',
    plage: true,
    description:
      'Navettes, séminaires, groupes constitués, rotations sur la journée.',
  },
  {
    nom: 'Autocar',
    prefixe: 'jusqu\'à',
    nombre: '120',
    plage: false,
    description:
      'Grands groupes, congrès, événements d\'entreprise, mariages.',
  },
];

// Villes du signal local. Aucune adresse postale : l'entite est un
// service-area business, exactement comme la fiche Google Business qui
// n'affiche aucune adresse et ne declare que des zones desservies. Le NAP
// publie ici se limite donc au nom et au telephone, a l'identique.
//
// Une seule liste alimente l'affichage ET le areaServed du JSON-LD : une
// zone visible que le balisage ignore, ou l'inverse, est un signal local
// incoherent.
//
// Des departements et non des communes. Une liste de villes, meme longue,
// se lit comme un perimetre ferme : le visiteur qui ne trouve pas la sienne
// en conclut qu'on n'y va pas. Le modele est la coordination de
// transporteurs partenaires, il n'a pas de frontiere communale. Le
// departement est la maille qui dit le plus large sans rien promettre de
// faux.
//
// Loire-Atlantique en premier, c'est le coeur d'activite. Ensuite les
// limitrophes des Pays de la Loire, puis la Bretagne, puis la Sarthe.
const DEPARTEMENTS = [
  { numero: '44', nom: 'Loire-Atlantique' },
  { numero: '85', nom: 'Vendée' },
  { numero: '49', nom: 'Maine-et-Loire' },
  { numero: '56', nom: 'Morbihan' },
  { numero: '35', nom: 'Ille-et-Vilaine' },
  { numero: '53', nom: 'Mayenne' },
  { numero: '72', nom: 'Sarthe' },
];

/*
  Les 3 etapes.

  Alignees sur la section #comment-ca-marche de l'accueil, qui suit deja le
  meme parcours brief -> dimensionnement -> jour J. Seule l'etape 02 differe :
  l'accueil dit "on assemble et on orchestre", sans delai ; ici le devis sous
  24h est la promesse de la page, elle figure deja dans le hero et la meta.
  Ce n'est pas une contradiction mais un engagement supplementaire, assume
  parce que la page est une page de conversion.

  Le registre est volontairement plus direct qu'a l'accueil ("vous nous dites"
  et non "vous nous briefez") : l'audience arrive de la publicite, elle a
  45-65 ans, elle ne connait pas la marque.
*/
const ETAPES = [
  {
    num: '01',
    texte: 'Vous nous dites combien vous êtes, quand et où',
  },
  {
    num: '02',
    texte: 'Nous vous envoyons un devis sous 24h avec le véhicule adapté',
  },
  {
    num: '03',
    texte: 'Le jour J, le chauffeur est sur place, vous n\'avez rien à gérer',
  },
];

/*
  FAQ de la page.

  Source unique : le JSON-LD FAQPage plus bas est genere depuis ce meme
  tableau, le texte affiche et le texte balise ne peuvent donc pas diverger.

  Trois contraintes tenues ici :

  1. Aucun prix chiffre. Le devis gratuit sous 24h est la seule reponse
     tarifaire, coherente avec faq.ts qui renvoie deja tout au devis.

  2. Vocabulaire du bloc VEHICULES. C'est dans ces reponses que le possessif
     revient le plus naturellement ("nos autocars", "nos chauffeurs") : rien
     n'est possede, on ecrit "nous mobilisons" et "le chauffeur".

  3. Coherence avec l'existant, verifiee ligne a ligne :
     - faq.ts:34 annonce "de quelques personnes a plusieurs centaines", alors
       que la page plafonne l'autocar a 120. Ce n'est contradictoire qu'en
       apparence : 120 est une capacite par vehicule, plusieurs centaines une
       capacite par dispositif. La reponse 2 le dit explicitement, sinon les
       deux pages se contredisent aux yeux du lecteur comme du moteur.
     - faq.ts:39 couvre "Nantes, Loire-Atlantique et tout le Grand Ouest" :
       la reponse 4 reprend ce perimetre a l'identique.
     - le delai de la reponse 6 recopie le tableau de
       blog/delai-reservation-autocar-evenement-entreprise.md (2 a 4 semaines
       van/minibus creuse, 1 a 2 mois autocar creuse, 3 a 4 mois haute
       saison). Ne pas modifier l'un sans l'autre.
*/
const FAQ_LOCATION = [
  {
    question: 'Combien coûte la location d\'un autocar avec chauffeur à Nantes ?',
    reponse:
      'Le tarif dépend de la distance, de la durée de mise à disposition et du véhicule mobilisé. Nous établissons un devis gratuit sous 24h après un point sur votre trajet, vos horaires et le nombre de passagers.',
  },
  {
    question: 'Quelle est la capacité maximale d\'un autocar ?',
    reponse:
      'Un autocar accueille jusqu\'à 120 passagers. Au-delà, nous mobilisons plusieurs véhicules coordonnés sur le même dispositif, ce qui permet de transporter plusieurs centaines de personnes sur un même événement.',
  },
  {
    question: 'Peut-on louer un minibus avec chauffeur pour une journée entière ?',
    reponse:
      'Oui, la mise à disposition à la journée est possible sur un minibus de 15 à 22 places. Le véhicule et le chauffeur restent affectés à votre groupe pour toutes les rotations prévues, et l\'amplitude horaire est intégrée au devis.',
  },
  {
    question: 'Intervenez-vous en dehors de Nantes ?',
    reponse:
      'Oui, dans sept départements : Loire-Atlantique, Vendée, Maine-et-Loire, Morbihan, Ille-et-Vilaine, Mayenne et Sarthe. Saint-Nazaire, La Baule, Pornic, Guérande, Clisson et Châteaubriant sont desservies comme le reste de la zone, et les trajets longue distance au départ de la région sont également organisés.',
  },
  {
    question: 'Proposez-vous des navettes pour un mariage ou un événement privé ?',
    reponse:
      'Oui. Navettes entre la cérémonie, la réception et les lieux d\'hébergement, avec les rotations et les retours de nuit. Nous sélectionnons le véhicule adapté au nombre d\'invités, de la berline à l\'autocar.',
  },
  {
    question: 'Sous quel délai faut-il réserver ?',
    reponse:
      'Comptez 2 à 4 semaines pour un van ou un minibus en période creuse, et 1 à 2 mois pour un autocar. De mai à septembre, ainsi qu\'à la rentrée, prévoyez 3 à 4 mois. Une demande de dernière minute reste possible : appelez-nous, nous vérifions les disponibilités immédiatement.',
  },
];

/*
  FORMULAIRE.

  Rien de la chaine d'envoi n'est reecrit ici. La cle Web3Forms, la page de
  confirmation et la logique complete (envoi -> marqueur sessionStorage ->
  redirection -> push generate_lead sur /devis-envoye) viennent de
  src/lib/devisEnvoi.ts, exactement comme pour le formulaire de l'accueil.
  Le suivi de conversion Google Ads depend de cette sequence : elle a une
  implementation et une seule.

  Seul le SUJET differe de celui de l'accueil, et c'est volontaire : c'est lui
  qui, dans la boite de reception, distingue un lead issu de la campagne payee
  d'un lead organique. Sans cette distinction, impossible d'attribuer le cout
  au clic aux demandes qu'il produit reellement.
*/
const SUJET_FORMULAIRE =
  'Demande de devis location avec chauffeur (campagne Google Ads)';

/*
  Tranches de passagers. Bornes calees sur les capacites annoncees dans la
  section VEHICULES : 1 a 8 couvre berline et van, 9 a 20 le minibus, 21 a 50
  le minibus haut et le petit autocar, 51 a 120 l'autocar. Un visiteur qui a
  lu les cartes retrouve ses reperes.

  Un select plutot qu'un champ nombre : sur mobile, avec une audience de 45 a
  65 ans, choisir dans une liste courte est plus sur que saisir un chiffre au
  clavier, et une tranche suffit pour dimensionner un devis.
*/
const TRANCHES_PASSAGERS = ['1 à 8', '9 à 20', '21 à 50', '51 à 120'];

// JSON-LD propre a la page. Fourni en @graph et rattache au meme @id
// d'entite que le reste du site, pour que les moteurs reconcilient une
// seule entite et non deux.
const jsonLd = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'LocalBusiness',
      '@id': `${SITE_DATA.url}#organization`,
      name: SITE_DATA.name,
      url: SITE_DATA.url,
      description,
      telephone: SITE_DATA.telephone,
      // Le balisage suit l'affichage a l'identique : les 7 departements
      // montres, plus la region qui les englobe pour la majorite d'entre
      // eux. Aucune City : declarer 28 communes quand la page en montre
      // zero produirait exactement l'incoherence que ce balisage doit eviter.
      areaServed: [
        { '@type': 'State', name: 'Pays de la Loire' },
        ...DEPARTEMENTS.map((d) => ({
          '@type': 'AdministrativeArea',
          name: d.nom,
        })),
      ],
      geo: {
        '@type': 'GeoCoordinates',
        latitude: SITE_DATA.geo.latitude,
        longitude: SITE_DATA.geo.longitude,
      },
      priceRange: SITE_DATA.priceRange,
    },
    /*
      FAQPage de la page, generee depuis FAQ_LOCATION : le texte balise est
      par construction celui qui est affiche, condition posee par Google.

      Pas de conflit avec le LocalBusiness ci-dessus : ce sont deux noeuds
      de types differents dans un meme @graph, ce que schema.org prevoit.
      Le LocalBusiness porte un @id partage avec le reste du site pour que
      les moteurs reconcilient une seule entite ; la FAQPage n'en porte pas,
      elle n'a pas a etre referencee ailleurs.

      Pas de conflit non plus avec la FAQPage du layout : cette page passe
      la prop `jsonLd`, qui remplace tout le graphe par defaut. Le faqNode
      de Base.astro, alimente par faq.ts, n'est donc pas emis ici. Une seule
      FAQPage par page, et ses 6 questions sont distinctes de celles de
      l'accueil : aucun doublon de balisage entre les deux URL.
    */
    {
      '@type': 'FAQPage',
      mainEntity: FAQ_LOCATION.map((item) => ({
        '@type': 'Question',
        name: item.question,
        acceptedAnswer: {
          '@type': 'Answer',
          text: item.reponse,
        },
      })),
    },
  ],
};
---

<Base title={title} description={description} jsonLd={jsonLd} headerTheme="dark">
  <!-- 1. HERO : promesse location vehicule avec chauffeur + appel en CTA
       principal. Aucune image de fond, aucun reveal, aucune animation
       d'entree : le trafic est paye, le premier rendu ne doit dependre
       d'aucun telechargement ni d'aucun script. Le LCP est le H1 texte. -->
  <section
    id="hero"
    data-tone="dark"
    class="relative overflow-hidden bg-green-900 text-on-dark"
  >
    <div class="hero-glow absolute inset-0" aria-hidden="true"></div>

    <div class="container relative z-10 pb-14 pt-28 sm:pt-32 lg:pb-24 lg:pt-40">
      <h1 class="hero-title max-w-[16ch]">
        Location d'autocar, bus et minibus avec chauffeur à Nantes
      </h1>

      <!-- L'accroche s'ouvre sur le Grand Ouest et non sur le seul 44 : la
           section Zone desservie annonce sept departements, une accroche
           limitee a un departement la contredirait des la premiere ligne de
           la page. Nantes et Saint-Nazaire restent nommes, ce sont les
           reperes locaux que cherchent les requetes payantes ; ils ouvrent
           maintenant sur un perimetre au lieu de le fermer. -->
      <p class="hero-subtitle mt-5 max-w-[46ch]">
        De la berline 3 places à l'autocar 120 places. Nantes, Saint-Nazaire
        et tout le Grand Ouest.
      </p>

      <!-- CTA. Ordre du DOM = ordre mobile : le bouton d'appel est premier,
           c'est l'action que fait reellement cette audience. -->
      <div class="mt-8 flex flex-col gap-3 sm:flex-row sm:flex-wrap sm:items-center sm:gap-4">
        <a
          href={TEL_HREF}
          class="hero-cta-tel"
          aria-label={`Appeler Transport Événementiel Nantes au ${TEL_AFFICHE}`}
        >
          <svg
            class="h-6 w-6 shrink-0"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.8"
            stroke-linecap="round"
            stroke-linejoin="round"
            aria-hidden="true"
          >
            <path
              d="M6.6 10.8a15.1 15.1 0 0 0 6.6 6.6l2.2-2.2a1 1 0 0 1 1-.25 11.4 11.4 0 0 0 3.6.58 1 1 0 0 1 1 1V20a1 1 0 0 1-1 1A17 17 0 0 1 3 4a1 1 0 0 1 1-1h3.5a1 1 0 0 1 1 1 11.4 11.4 0 0 0 .58 3.6 1 1 0 0 1-.25 1Z"
            ></path>
          </svg>
          <span class="hero-cta-tel-number">{TEL_AFFICHE}</span>
        </a>

        <a href="#formulaire-devis" class="hero-cta-devis">
          Demander un devis
        </a>
      </div>

      <ul class="hero-reassurance mt-7">
        {
          REASSURANCE.map((item) => (
            <li>{item}</li>
          ))
        }
      </ul>
    </div>
  </section>

  <!-- 2. VEHICULES : berline, van, minibus, autocar, de 3 a 120 passagers.
       Section nommee "vehicules" et non "flotte" : le mot flotte appelle le
       possessif ("notre flotte"), qui est precisement interdit ici. Voir le
       bloc VEHICULES dans le frontmatter. -->
  <Section id="vehicules" tone="cream">
    <p class="type-eyebrow vehicules-eyebrow">Véhicules avec chauffeur</p>

    <h2 class="type-h2 mt-3 max-w-[18ch]">Quel véhicule pour votre groupe ?</h2>

    <!-- Le modele est enonce des la premiere phrase, sans detour. Un
         visiteur qui cherche un autocariste doit savoir tout de suite a qui
         il parle : un coordinateur, pas un depot de bus. -->
    <!-- 68ch et non 52 : a 52 la phrase tombait sur trois lignes en desktop
         avec un "J." orphelin sur la derniere. La contrainte ne joue de
         toute facon pas sous 640px, ou le container est plus etroit. -->
    <p class="vehicules-intro mt-5 max-w-[68ch]">
      Nous mobilisons les véhicules et les chauffeurs professionnels de nos
      transporteurs partenaires. Un seul interlocuteur, du devis au jour J.
    </p>

    <ul class="vehicules-liste mt-10">
      {
        VEHICULES.map((v) => (
          <li class="vehicule">
            <p class="vehicule-capacite">
              <span class="vehicule-prefixe">{v.prefixe}</span>
              <span
                class:list={[
                  'type-stat',
                  'vehicule-nombre',
                  v.plage && 'vehicule-nombre--plage',
                ]}
              >
                {v.nombre}
              </span>
              <span class="vehicule-unite">passagers</span>
            </p>

            <h3 class="vehicule-nom">{v.nom}</h3>
            <p class="vehicule-description">{v.description}</p>
          </li>
        ))
      }
    </ul>

    <!-- Aide au choix. Le visiteur qui hesite entre minibus et autocar est
         exactement celui qui repart sans rien faire : on lui donne l'appel
         comme sortie, au moment ou l'hesitation se produit. -->
    <p class="vehicules-aide mt-10">
      Vous ne savez pas quel véhicule choisir ?{' '}
      <a
        href={TEL_HREF}
        class="vehicules-aide-lien"
        aria-label={`Appeler Transport Événementiel Nantes au ${TEL_AFFICHE}`}
      >
        Appelez-nous
      </a>, on vous oriente en deux minutes.
    </p>
  </Section>

  <!-- 3. ZONE DESSERVIE : les sept departements du Grand Ouest -->
  <Section id="zone-desservie" tone="cream-soft">
    <p class="type-eyebrow zone-eyebrow">Zone desservie</p>

    <h2 class="type-h2 mt-3 max-w-[20ch]">
      Nous intervenons dans tout le Grand Ouest
    </h2>

    <!-- Le numero porte la lecture, exactement comme la capacite sur les
         cartes vehicules : dore, en police d'affichage, nettement plus gros
         que le nom. C'est ce que l'oeil accroche en premier et c'est ce qui
         se reconnait le plus vite, un Vendeen sait qu'il est en 85 avant de
         lire "Vendee".

         Aucun lien, donc aucune pastille bordee ni fond colore qui ferait
         croire a un element cliquable. -->
    <ul class="zone-departements mt-8">
      {
        DEPARTEMENTS.map((d) => (
          <li class="zone-departement">
            <span class="zone-numero">{d.numero}</span>
            <span class="zone-nom">{d.nom}</span>
          </li>
        ))
      }
    </ul>

    <!-- Le repli « Voir toutes les villes » a disparu avec les 28 communes :
         sept entrees tiennent sans depasser un ecran, un bouton pour les
         montrer n'aurait plus d'objet.

         La phrase qui citait la gare de Nantes, l'aeroport et la Cite des
         Congres est retiree elle aussi. Nommer trois points nantais juste
         sous une carte qui annonce sept departements laissait entendre que
         les departs partent de Nantes, ce qui referme le perimetre qu'on
         vient d'ouvrir. -->
    <p class="zone-points mt-8 max-w-[60ch]">
      Départ et retour depuis le lieu de votre choix, dans toute la zone.
    </p>
  </Section>

  <!-- 4. COMMENT CA MARCHE : de la demande au jour J.
       Meme parcours que la section homonyme de l'accueil, en trois etapes et
       avec le meme traitement de numero dore. Le texte est plus court et
       plus direct : ici on convertit, on ne presente pas une methode. -->
  <Section id="comment-ca-marche" tone="cream">
    <p class="type-eyebrow etapes-eyebrow">Comment ça marche</p>

    <h2 class="type-h2 mt-3 max-w-[20ch]">Trois étapes, et c'est réglé</h2>

    <ol class="etapes-liste mt-10">
      {
        ETAPES.map((etape) => (
          <li class="etape">
            <span class="etape-num" aria-hidden="true">{etape.num}</span>
            <p class="etape-texte">{etape.texte}</p>
          </li>
        ))
      }
    </ol>
  </Section>

  <!-- 5. FAQ : questions de location, balisees en FAQPage depuis le meme
       tableau FAQ_LOCATION que l'affichage.

       <details> natif : accordeon sans une ligne de JavaScript, donc aucun
       risque que la reponse reste inaccessible si le script echoue sur du
       trafic paye. Clavier et lecteurs d'ecran sont geres par le navigateur.
       La premiere question est ouverte : sur une audience 45-65 ans, c'est
       ce qui signale que les autres s'ouvrent aussi. -->
  <Section id="faq" tone="cream-soft">
    <p class="type-eyebrow faq-eyebrow">Questions fréquentes</p>

    <h2 class="type-h2 mt-3 max-w-[20ch]">Ce qu'on nous demande le plus</h2>

    <div class="faq-liste mt-10">
      {
        FAQ_LOCATION.map((item, index) => (
          <details class="faq-item" open={index === 0}>
            <summary class="faq-question">
              <span>{item.question}</span>
              <svg
                class="faq-chevron"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                aria-hidden="true"
              >
                <path d="M6 9l6 6 6-6" />
              </svg>
            </summary>
            <p class="faq-reponse">{item.reponse}</p>
          </details>
        ))
      }
    </div>

    <p class="faq-aide mt-10">
      Votre question n'est pas là ?{' '}
      <a
        href={TEL_HREF}
        class="faq-aide-lien"
        aria-label={`Appeler Transport Événementiel Nantes au ${TEL_AFFICHE}`}
      >
        Appelez-nous
      </a>, on vous répond directement.
    </p>
  </Section>

  <!-- 6. FORMULAIRE : demande de devis, conversion finale.

       Le <form> porte data-devis-form : c'est ce seul attribut qui le branche
       sur la chaine d'envoi partagee (src/lib/devisEnvoi.ts), la meme que le
       formulaire de l'accueil. Ne pas le retirer, le formulaire enverrait
       alors en POST classique et la conversion Google Ads ne serait plus
       comptee. -->
  <Section id="formulaire-devis" tone="dark">
    <div class="form-entete">
      <h2 class="type-h2">Demandez votre devis</h2>
      <p class="form-sous-titre mt-4">
        Quelques informations suffisent pour dimensionner le véhicule et vous
        chiffrer le trajet.
      </p>
    </div>

    <form
      class="form-devis mt-10"
      novalidate
      data-devis-form
      data-sent-href={PAGE_ENVOI}
    >
      <!-- Champs caches Web3Forms -->
      <input type="hidden" name="access_key" value={WEB3FORMS_KEY} />
      <input type="hidden" name="subject" value={SUJET_FORMULAIRE} />
      <input
        type="hidden"
        name="from_name"
        value="Landing location avec chauffeur"
      />

      <!-- Honeypot anti-spam : doit rester vide. C'est le dispositif du site,
           il n'y a pas de Turnstile a repliquer. Sorti de l'ecran en CSS et
           retire du parcours clavier comme de l'arbre d'accessibilite. -->
      <input
        type="checkbox"
        name="botcheck"
        class="form-honeypot"
        tabindex="-1"
        autocomplete="off"
        aria-hidden="true"
      />

      <!-- Nom + Telephone. Le telephone est le canal de rappel privilegie de
           cette audience, il vient donc juste apres le nom. -->
      <div class="form-duo">
        <div class="form-champ">
          <label class="form-label" for="loc-nom">
            Nom <span class="form-requis" aria-hidden="true">*</span>
          </label>
          <input
            id="loc-nom"
            name="Nom"
            type="text"
            autocomplete="name"
            class="form-input"
            required
          />
        </div>

        <div class="form-champ">
          <label class="form-label" for="loc-tel">
            Téléphone <span class="form-requis" aria-hidden="true">*</span>
          </label>
          <!-- inputmode="tel" en plus de type="tel" : c'est lui qui garantit
               le pave numerique a l'ouverture sur Android comme sur iOS. -->
          <input
            id="loc-tel"
            name="Téléphone"
            type="tel"
            inputmode="tel"
            autocomplete="tel"
            class="form-input"
            required
          />
        </div>
      </div>

      <!-- Email + Societe -->
      <div class="form-duo">
        <div class="form-champ">
          <label class="form-label" for="loc-email">
            Email <span class="form-requis" aria-hidden="true">*</span>
          </label>
          <input
            id="loc-email"
            name="Email"
            type="email"
            inputmode="email"
            autocomplete="email"
            class="form-input"
            required
          />
        </div>

        <div class="form-champ">
          <label class="form-label" for="loc-societe">
            Société <span class="form-requis" aria-hidden="true">*</span>
          </label>
          <input
            id="loc-societe"
            name="Société"
            type="text"
            autocomplete="organization"
            class="form-input"
            required
          />
        </div>
      </div>

      <!-- Nombre de passagers + Date -->
      <div class="form-duo">
        <div class="form-champ">
          <label class="form-label" for="loc-passagers">
            Nombre de passagers
            <span class="form-requis" aria-hidden="true">*</span>
          </label>
          <div class="form-select-enveloppe">
            <select
              id="loc-passagers"
              name="Nombre de passagers"
              class="form-input form-select"
              required
            >
              <option value="" disabled selected>Choisir...</option>
              {
                TRANCHES_PASSAGERS.map((tranche) => (
                  <option value={tranche}>{tranche} passagers</option>
                ))
              }
            </select>
            <span class="form-select-fleche" aria-hidden="true"></span>
          </div>
        </div>

        <div class="form-champ">
          <label class="form-label" for="loc-date">
            Date de la prestation
          </label>
          <input
            id="loc-date"
            name="Date de la prestation"
            type="date"
            class="form-input"
          />
        </div>
      </div>

      <!-- Trajet ou besoin, optionnel. Le seul champ libre : on ne demande pas
           au visiteur de rediger, on lui laisse la possibilite de le faire. -->
      <div class="form-champ">
        <label class="form-label" for="loc-trajet">
          Trajet ou besoin <span class="form-facultatif">(facultatif)</span>
        </label>
        <textarea
          id="loc-trajet"
          name="Trajet ou besoin"
          rows="4"
          class="form-input form-textarea"
          placeholder="Ex : Nantes gare vers Château de..., aller-retour le 12 juin"
        ></textarea>
      </div>

      <!-- Message d'erreur. Deux classes, deux roles distincts :
           .devis-error est le CONTRAT avec le module d'envoi, qui cherche cet
           element a l'interieur du <form> ; .form-error ne fait que l'habiller
           pour cette page. Retirer .devis-error rendrait toute erreur
           silencieuse, le visiteur cliquerait sans rien voir se passer. -->
      <p
        class="devis-error form-error"
        role="alert"
        aria-live="assertive"
        hidden
      >
      </p>

      <div class="form-actions">
        <ButtonArrow
          variant="solid"
          type="submit"
          label="Envoyer ma demande"
          class="devis-submit form-submit"
        />

        <!-- Rappel du delai et sortie telephonique. Le meme engagement que le
             hero, la meta et l'etape 02 : "sous 24h". "ouvrees" est la seule
             precision ajoutee, elle vaut aussi sur /devis-envoye. -->
        <p class="form-delai">
          Réponse sous 24h ouvrées. Ou appelez directement le{' '}
          <a
            href={TEL_HREF}
            class="form-delai-tel"
            aria-label={`Appeler Transport Événementiel Nantes au ${TEL_AFFICHE}`}
          >
            {TEL_AFFICHE}
          </a>
        </p>
      </div>
    </form>
  </Section>

  <!-- Barre d'appel fixe, mobile uniquement. Prend le relais du CTA du hero
       des qu'il sort de l'ecran. Voir CallBar.astro pour les conditions
       d'affichage. -->
  <CallBar />
</Base>

<script>
  /*
    Branchement du formulaire sur la chaine d'envoi partagee. Rien n'est
    reimplemente ici : c'est le module que le formulaire de l'accueil appelle,
    au mot pres. Web3Forms, marqueur sessionStorage, redirection vers
    /devis-envoye, puis push generate_lead par la page de confirmation.
  */
  import { brancherFormulairesDevis } from '../lib/devisEnvoi.ts';

  brancherFormulairesDevis();
</script>

<style>
  /* ============ HERO ============
     Ecrit mobile d'abord : les regles de base ciblent 375px, les media
     queries montent ensuite. 83 % du trafic arrive sur mobile.

     Aucune image, aucun degrade lourd : un halo radial en CSS pur donne de
     la profondeur sans une seule requete reseau. */

  .hero-glow {
    background:
      radial-gradient(
        ellipse 90% 60% at 15% 0%,
        rgba(42, 70, 54, 0.55) 0%,
        transparent 70%
      ),
      radial-gradient(
        ellipse 70% 50% at 100% 100%,
        rgba(201, 162, 75, 0.1) 0%,
        transparent 65%
      );
  }

  /* H1. Pas de uppercase, contrairement au hero de l'accueil : le titre est
     long (57 caracteres) et la capitale ajoute environ 12 % de largeur, donc
     deux lignes de plus sur un ecran de 375px. En bas de casse il tient plus
     haut dans l'ecran et se lit plus vite, ce qui compte pour une audience
     de 45 a 65 ans. */
  .hero-title {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: clamp(1.95rem, 7vw, 3.4rem);
    line-height: 1.08;
    letter-spacing: -0.015em;
    color: var(--on-dark);
  }

  /* Sous-titre. Couleur posee en CSS et non via text-on-dark/[0.92] :
     l'opacite arbitraire de Tailwind ne produit aucune regle sur une
     variable non-RGB (verifie dans le CSS compile, la classe n'existe pas).
     Voir CLAUDE.md. */
  .hero-subtitle {
    font-family: var(--font-body);
    font-weight: 400;
    /* 17px minimum sur mobile, au-dessus du plancher de 16px demande. */
    font-size: 1.0625rem;
    line-height: 1.6;
    color: var(--on-dark);
    opacity: 0.92;
  }

  /* ---- CTA 1 : appel telephonique, action dominante ----
     Pleine largeur sur mobile, hauteur 60px (le minimum demande etait 48px),
     numero a 1.5rem. C'est l'element le plus lourd visuellement de la page,
     volontairement. */
  .hero-cta-tel {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.7rem;
    width: 100%;
    min-height: 60px;
    padding: 0.75rem 1.5rem;
    border-radius: 9999px;
    background-color: var(--gold);
    color: var(--ink);
    text-decoration: none;
    transition: background-color 300ms var(--ease);
  }

  .hero-cta-tel-number {
    font-family: var(--font-body);
    font-weight: 600;
    font-size: 1.5rem;
    line-height: 1;
    letter-spacing: 0.01em;
    /* Le numero ne doit jamais se couper en deux lignes. */
    white-space: nowrap;
  }

  /* ---- CTA 2 : devis, secondaire ---- */
  .hero-cta-devis {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    min-height: 52px;
    padding: 0.75rem 1.75rem;
    border-radius: 9999px;
    border: 1px solid var(--line-on-dark);
    font-family: var(--font-body);
    font-weight: 500;
    font-size: 1rem;
    line-height: 1;
    color: var(--on-dark);
    text-decoration: none;
    transition:
      border-color 300ms var(--ease),
      background-color 300ms var(--ease);
  }

  /* ---- Reassurance ----
     Les trois arguments ne tiennent pas sur une ligne a 375px (environ 62
     caracteres pour 335px utiles). Plutot qu'une ligne a separateurs qui se
     casse n'importe ou, on empile en liste sur mobile, un point dore devant
     chaque item, et on revient a la ligne separee des 640px ou la place
     existe.

     Les puces sont en ::before : le point n'est ni lu par les lecteurs
     d'ecran, ni copie avec le texte. */
  .hero-reassurance {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    margin: 0;
    padding: 0;
    list-style: none;
    font-family: var(--font-body);
    font-weight: 400;
    font-size: 1rem;
    line-height: 1.5;
    color: var(--on-dark);
    opacity: 0.88;
  }

  .hero-reassurance li {
    display: flex;
    align-items: center;
    gap: 0.6rem;
  }

  .hero-reassurance li::before {
    content: '';
    flex: none;
    width: 4px;
    height: 4px;
    border-radius: 9999px;
    background-color: var(--gold);
  }

  /* ---- A partir de 640px : les CTA passent cote a cote et se dimensionnent
       a leur contenu, la reassurance revient sur une seule ligne. ---- */
  @media (min-width: 640px) {
    .hero-cta-tel,
    .hero-cta-devis {
      width: auto;
    }

    .hero-reassurance {
      flex-direction: row;
      flex-wrap: wrap;
      align-items: center;
      gap: 0.4rem 0.85rem;
    }

    /* En ligne, le premier item n'a pas de separateur devant lui. */
    .hero-reassurance li:first-child::before {
      display: none;
    }
  }

  /* Le hover n'existe pas sur mobile. On le reserve aux appareils qui
     pointent vraiment, pour eviter les etats colles au tap. */
  @media (hover: hover) {
    .hero-cta-tel:hover {
      background-color: var(--gold-soft);
    }
    .hero-cta-devis:hover {
      border-color: var(--gold);
      background-color: rgba(237, 231, 218, 0.06);
    }
  }

  /* ============ VEHICULES ============
     Aucune image disponible, et aucune image d'illustration : montrer un
     autocar qui n'est pas celui qu'on fournit poserait le meme probleme
     que d'ecrire "notre flotte". La capacite porte donc seule la charge
     visuelle, en typographie.

     Capacite au-dessus du texte, et non dans une colonne a gauche : la
     colonne testee ne laissait que 203px au descriptif a 375px, qui partait
     alors sur quatre lignes. En pleine largeur il tient sur deux, pour une
     hauteur de carte identique. A encombrement egal, on prend la version
     lisible.

     Pas de carrousel horizontal : il masque une partie de l'offre derriere
     un geste, et cette audience ne le fait pas. Les quatre capacites
     doivent etre lisibles d'un seul coup d'oeil. */

  .vehicules-eyebrow {
    color: var(--gold);
  }

  .vehicules-intro {
    font-family: var(--font-body);
    font-size: 1.0625rem;
    line-height: 1.65;
    color: var(--ink-muted);
  }

  .vehicules-liste {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .vehicule {
    padding: 1.25rem;
    border: 1px solid var(--line);
    border-radius: 1rem;
    background-color: var(--cream-soft);
  }

  /* Capacite sur une ligne, alignee sur la ligne de base : le nombre est
     trois fois plus gros que ce qui l'entoure, mais l'ensemble se lit
     quand meme comme une phrase, "jusqu'a 3 passagers". */
  .vehicule-capacite {
    display: flex;
    align-items: baseline;
    flex-wrap: wrap;
    gap: 0.35rem;
    margin: 0;
  }

  .vehicule-prefixe,
  .vehicule-unite {
    font-family: var(--font-body);
    font-weight: 500;
    font-size: 0.9375rem;
    line-height: 1.2;
    color: var(--ink-muted);
  }

  .vehicule-nombre {
    color: var(--gold);
    /* Une capacite ne se coupe jamais en deux lignes, "15 a 22" comprise. */
    white-space: nowrap;
  }

  /* Une fourchette occupe six caracteres la ou un plafond en occupe un a
     trois. Sans ce cran en dessous, la ligne du minibus se casse dans la
     grille a deux colonnes. */
  .vehicule-nombre--plage {
    font-size: clamp(1.6rem, 3vw, 2.1rem);
  }

  .vehicule-nom {
    margin-top: 0.6rem;
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1.25rem;
    line-height: 1.2;
    color: var(--ink);
  }

  .vehicule-description {
    margin-top: 0.35rem;
    font-family: var(--font-body);
    font-size: 0.9375rem;
    line-height: 1.5;
    color: var(--ink-muted);
  }

  /* ---- Aide au choix ---- */
  .vehicules-aide {
    font-family: var(--font-body);
    font-size: 1.0625rem;
    line-height: 1.6;
    color: var(--ink);
  }

  /* Lien d'appel en plein texte. La cible est modeste, mais elle ne porte
     pas l'appel a elle seule : a ce niveau de scroll la barre d'appel fixe
     est affichee en bas de l'ecran, avec ses 56px de haut sur toute la
     largeur. Ce lien est un rappel dans le fil de la lecture, pas le
     dispositif principal. Souligne et dore : reconnaissable comme lien sans
     dependre de la seule couleur. */
  .vehicules-aide-lien {
    display: inline-block;
    padding: 0.15rem 0.1rem;
    color: var(--ink);
    font-weight: 600;
    text-decoration: underline;
    text-decoration-color: var(--gold);
    text-decoration-thickness: 2px;
    text-underline-offset: 3px;
    transition: color 300ms var(--ease);
  }

  @media (min-width: 640px) {
    /* Deux colonnes des que la place existe : la gamme se lit alors en un
       seul bloc, sans scroll. */
    .vehicules-liste {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 1rem;
    }

    .vehicule {
      padding: 1.5rem;
    }
  }

  @media (hover: hover) {
    .vehicules-aide-lien:hover {
      color: var(--gold);
    }
  }

  /* ============ ZONE DESSERVIE ============ */

  .zone-eyebrow,
  .etapes-eyebrow,
  .faq-eyebrow {
    color: var(--gold);
  }

  /* Grille de departements.

     margin-bottom seul, jamais `margin: 0` : le style scope d'Astro pese
     (0,2,0) contre (0,1,0) pour une utilitaire Tailwind, un raccourci
     `margin` ecraserait le `mt-8` pose sur l'element et collerait la liste
     au titre. */
  .zone-departements {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1.75rem 1.5rem;
    list-style: none;
    padding: 0;
    margin-bottom: 0;
  }

  .zone-departement {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
  }

  /* Meme traitement que la capacite des cartes vehicules : le nombre est
     dore, en police d'affichage, et nettement plus gros que ce qui
     l'accompagne. Il ne se coupe jamais. */
  .zone-numero {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: clamp(2rem, 4.2vw, 2.6rem);
    line-height: 1;
    color: var(--gold);
    white-space: nowrap;
  }

  .zone-nom {
    font-family: var(--font-body);
    font-size: 1rem;
    font-weight: 500;
    line-height: 1.35;
    color: var(--ink);
  }

  @media (min-width: 640px) {
    .zone-departements {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
  }

  /* Sept colonnes et non quatre a partir de 1024px : les sept departements
     tiennent alors sur une seule ligne, et une ligne unique se lit comme un
     territoire continu la ou une grille 4 + 3 laisse un rang incomplet qui
     donne l'impression d'une liste tronquee. Le seuil est mesure : c'est la
     largeur a partir de laquelle « Loire-Atlantique » et « Ille-et-Vilaine »,
     les deux noms les plus longs, tiennent sans se couper. */
  @media (min-width: 1024px) {
    .zone-departements {
      grid-template-columns: repeat(7, minmax(0, 1fr));
      gap: 1.5rem 1rem;
    }
    .zone-nom {
      font-size: 0.9375rem;
    }
  }

  .zone-points {
    font-family: var(--font-body);
    font-size: 1rem;
    line-height: 1.6;
    color: var(--ink-muted);
  }

  /* ============ COMMENT CA MARCHE ============
     Le numero est en colonne fixe a gauche : les trois textes s'alignent
     alors sur une meme verticale, ce qui fait lire la sequence comme une
     progression et non comme trois blocs. Meme traitement dore et meme
     police display qu'a l'accueil, pour que ce soit visiblement le meme
     process raconte deux fois. */

  /* Voir la note sur .zone-departements : margin-bottom seul, pas de raccourci
     `margin`, sinon le `mt-10` de l'element saute. */
  .etapes-liste {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    list-style: none;
    padding: 0;
    margin-bottom: 0;
  }

  .etape {
    display: grid;
    grid-template-columns: 3.25rem 1fr;
    align-items: baseline;
    gap: 0.25rem;
  }

  .etape-num {
    font-family: var(--font-display);
    font-optical-sizing: auto;
    font-weight: 700;
    font-size: 1.75rem;
    line-height: 1.2;
    letter-spacing: -0.01em;
    font-variant-numeric: tabular-nums;
    color: var(--gold);
  }

  .etape-texte {
    font-family: var(--font-body);
    font-size: 1.0625rem;
    font-weight: 500;
    line-height: 1.5;
    color: var(--ink);
  }

  @media (min-width: 640px) {
    .etape {
      grid-template-columns: 4.5rem 1fr;
      gap: 0.5rem;
    }

    .etape-num {
      font-size: 2.25rem;
    }

    .etape-texte {
      font-size: 1.1875rem;
    }
  }

  /* ============ FAQ ============ */

  .faq-liste {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .faq-item {
    border: 1px solid var(--line);
    border-radius: 1rem;
    background-color: var(--cream-soft);
    overflow: hidden;
  }

  /* Le marqueur natif du <summary> est retire des deux facons : `list-style`
     couvre Firefox et Chrome, `::-webkit-details-marker` l'ancien Safari.
     Sans la seconde, un triangle noir subsiste sur iOS. */
  .faq-question {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    /* 56px de haut au minimum une fois le padding compte : cible tactile
       confortable, la meme que le bouton d'appel. */
    min-height: 56px;
    padding: 1rem 1.25rem;
    cursor: pointer;
    list-style: none;
    font-family: var(--font-body);
    font-size: 1.0625rem;
    font-weight: 600;
    line-height: 1.4;
    color: var(--ink);
  }

  .faq-question::-webkit-details-marker {
    display: none;
  }

  .faq-chevron {
    width: 1.25rem;
    height: 1.25rem;
    flex-shrink: 0;
    color: var(--gold);
    transition: transform 260ms var(--ease);
  }

  .faq-item[open] .faq-chevron {
    transform: rotate(180deg);
  }

  /* Le padding haut est nul : la reponse doit sembler attachee a sa
     question, pas flotter au milieu de la carte. */
  .faq-reponse {
    padding: 0 1.25rem 1.25rem;
    margin: 0;
    font-family: var(--font-body);
    font-size: 1rem;
    line-height: 1.65;
    color: var(--ink-muted);
  }

  /* Focus visible sur le summary : c'est la seule cible clavier de la
     section, la perdre rendrait la FAQ inutilisable sans souris. */
  .faq-question:focus-visible {
    outline: 2px solid var(--gold);
    outline-offset: -2px;
  }

  .faq-aide {
    font-family: var(--font-body);
    font-size: 1.0625rem;
    line-height: 1.6;
    color: var(--ink);
  }

  .faq-aide-lien {
    display: inline-block;
    padding: 0.15rem 0.1rem;
    color: var(--ink);
    font-weight: 600;
    text-decoration: underline;
    text-decoration-color: var(--gold);
    text-decoration-thickness: 2px;
    text-underline-offset: 3px;
    transition: color 300ms var(--ease);
  }

  @media (min-width: 640px) {
    .faq-question {
      font-size: 1.125rem;
      padding: 1.125rem 1.5rem;
    }

    .faq-reponse {
      padding: 0 1.5rem 1.5rem;
      font-size: 1.0625rem;
    }
  }

  @media (hover: hover) {
    .faq-aide-lien:hover {
      color: var(--gold);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .faq-chevron {
      transition: none;
    }
  }

  /* ============ FORMULAIRE ============
     Fond sombre (--green-900) : les champs sont donc en creux, plus sombres
     que la section, avec une bordure claire faible. Contraste calibre pour
     une audience de 45 a 65 ans, sur mobile, souvent en exterieur.

     Regle structurante de tout ce bloc : 56px de hauteur minimale sur chaque
     controle, contre les 48px demandes. La marge de 8px absorbe le fait que
     Safari iOS et Chrome Android n'appliquent pas les paddings d'un <select>
     ni d'un <input type=date> de la meme facon ; caler pile a 48px ferait
     passer certains d'entre eux sous la cible. */

  .form-entete {
    max-width: 34rem;
  }

  .form-sous-titre {
    font-family: var(--font-body);
    font-size: 1.0625rem;
    line-height: 1.6;
    color: var(--on-dark);
    /* Pas de text-on-dark/[0.78] : Tailwind ne resout pas l'opacite arbitraire
       sur --on-dark, qui est un hexa et non des canaux RGB. Le rendu serait
       gris sombre au lieu de creme attenue. */
    opacity: 0.78;
  }

  .form-devis {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    max-width: 44rem;
  }

  /* Une colonne sur mobile. Les paires ne se forment qu'a partir de 640px :
     en dessous, deux champs cote a cote reduisent chacun a une largeur ou le
     libelle se casse et la cible tactile devient etroite. */
  .form-duo {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.25rem;
  }

  .form-champ {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  /* Libelle visible et permanent, jamais un placeholder seul : un placeholder
     disparait des la premiere frappe, et le visiteur qui verifie sa saisie ne
     sait plus ce qu'on lui demandait. */
  .form-label {
    font-family: var(--font-body);
    font-size: 0.9375rem;
    font-weight: 500;
    line-height: 1.4;
    color: var(--on-dark);
  }

  .form-requis {
    color: var(--gold);
    margin-left: 0.15rem;
  }

  .form-facultatif {
    font-weight: 400;
    opacity: 0.6;
  }

  .form-input {
    width: 100%;
    min-height: 56px;
    padding: 0.9rem 1rem;
    border: 1px solid var(--line-on-dark);
    border-radius: 0.5rem;
    background-color: var(--green-800);
    color: var(--on-dark);
    font-family: var(--font-body);
    /* 1rem strict : en dessous de 16px, iOS zoome automatiquement au focus et
       la page part de travers au premier champ touche. */
    font-size: 1rem;
    line-height: 1.4;
    transition:
      border-color 200ms var(--ease),
      background-color 200ms var(--ease);
  }

  .form-input::placeholder {
    color: var(--on-dark);
    opacity: 0.4;
  }

  .form-input:focus-visible {
    outline: none;
    border-color: var(--gold);
    background-color: var(--green-700);
  }

  /* Champ en erreur : la bordure ne suffit pas seule, elle double le message
     texte affiche en bas de formulaire, elle ne le remplace pas. Cible
     :user-invalid et non :invalid : un champ encore vierge n'est pas une
     erreur, seulement un champ pas encore rempli. */
  .form-input:user-invalid {
    border-color: rgba(210, 120, 105, 0.8);
  }

  .form-textarea {
    min-height: 7rem;
    resize: vertical;
  }

  /* Select : la fleche native est illisible sur fond sombre selon les
     navigateurs, on la remplace par un chevron trace en bordures. */
  .form-select-enveloppe {
    position: relative;
  }

  .form-select {
    appearance: none;
    -webkit-appearance: none;
    padding-right: 2.75rem;
  }

  .form-select-fleche {
    position: absolute;
    right: 1.15rem;
    top: 50%;
    width: 0.5rem;
    height: 0.5rem;
    border-right: 1.5px solid var(--gold);
    border-bottom: 1.5px solid var(--gold);
    transform: translateY(-70%) rotate(45deg);
    pointer-events: none;
  }

  /* L'icone de calendrier native est noire sur fond sombre : invisible.
     L'inversion la rend claire. */
  .form-input[type='date']::-webkit-calendar-picker-indicator {
    filter: invert(1);
    opacity: 0.6;
    cursor: pointer;
  }

  /* Honeypot. !important assume : la regle doit resister a toute utilitaire
     posee sur l'element, un champ piege visible serait rempli par un humain
     et sa demande serait rejetee silencieusement. */
  .form-honeypot {
    position: absolute !important;
    left: -9999px !important;
    opacity: 0 !important;
    height: 0 !important;
    width: 0 !important;
    pointer-events: none !important;
  }

  .form-error {
    margin: 0;
    padding: 0.9rem 1rem;
    border: 1px solid rgba(210, 120, 105, 0.45);
    border-radius: 0.5rem;
    background-color: rgba(180, 70, 60, 0.15);
    color: #f0c9c1;
    font-family: var(--font-body);
    font-size: 0.9375rem;
    line-height: 1.55;
  }

  .form-actions {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1rem;
    margin-top: 0.5rem;
  }

  /* Bouton pleine largeur sur mobile : c'est l'action de la section, elle ne
     doit pas se chercher. ButtonArrow donne deja 48px de haut (py-1.5 plus un
     rond de 36px), on monte a 56px pour rester au niveau des champs et de la
     barre d'appel.

     :global() obligatoire ici, et pas un simple `.form-submit` : le <button>
     est rendu par ButtonArrow, Astro ne pose donc PAS l'attribut de scope
     dessus. Une regle scopee compile en `.form-submit[data-astro-cid-...]`,
     ne matche jamais, et echoue en silence : le bouton restait a 48px et en
     largeur auto sans qu'aucune erreur ne le signale. On ancre la regle sur
     .form-actions, qui appartient a cette page et porte le scope : la portee
     reste locale, seule la cible traverse. */
  .form-actions :global(.form-submit) {
    width: 100%;
    justify-content: center;
    min-height: 56px;
  }

  /* Etat d'envoi. Le libelle passe deja a "Envoi..." cote script ; ce retrait
     de contraste dit en plus que le bouton n'est plus cliquable, sans quoi un
     second tap semble simplement ne rien faire. */
  .form-actions :global(.form-submit[disabled]) {
    opacity: 0.6;
    cursor: not-allowed;
    pointer-events: none;
  }

  .form-delai {
    font-family: var(--font-body);
    font-size: 0.9375rem;
    line-height: 1.55;
    color: var(--on-dark);
    opacity: 0.72;
  }

  /* Le numero reste un lien tel: sur toutes les tailles, y compris desktop :
     une partie de cette audience appelle depuis un mobile ou le site est
     ouvert en second onglet. Souligne pour qu'il se lise comme cliquable
     meme sans survol possible. */
  .form-delai-tel {
    color: var(--gold);
    text-decoration: underline;
    text-underline-offset: 0.2em;
    white-space: nowrap;
  }

  @media (min-width: 640px) {
    .form-duo {
      grid-template-columns: 1fr 1fr;
    }

    .form-actions :global(.form-submit) {
      width: auto;
    }

    .form-actions {
      gap: 1.25rem;
    }
  }

  @media (hover: hover) {
    .form-delai-tel:hover {
      color: var(--cream);
    }
  }
</style>
